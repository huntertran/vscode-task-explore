# Sample workspace

Demo folder opened by the **Run Extension** debug config so the Extension Development
Host has a workspace with tasks to display (the repo root can't be opened in the dev host
when it's already open in your main VS Code window).

Add tasks to `.vscode/tasks.json` here to test the Task Explorer view.
